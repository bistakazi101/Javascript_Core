// The pop methods pops the element returns the removed value of  the array
// And it changes the length property 

let numbers =[1,2,3,4,5];
const length = numbers.pop();
console.log(length);
console.log(numbers);
console.log(numbers.length);