// unary operators 
// unary + operator it does nothing 
// Unary plus (+) or minus (-) converts a non-numeric value into a number. 
  let x= 100
  console.log(+x) 


  // unary mins opeator 
  let y = -10
  console.log(-y)

// Increment and decrement operator 
let age = 25;
++age;

// Decrement operator 
let weight = 90;
weight = weight - 1;

console.log(weight); // 89
// postfixcx increment operator
let a = 5;
let b = a++; // Postfix increment: increments 'a' by 1, but 'b' gets the
//  original value of 'a' before the increment
console.log("a:", a); // Output: 6
console.log("b:", b); // Output: 5
